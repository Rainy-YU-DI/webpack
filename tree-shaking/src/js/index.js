// 引入
import '../css/index.css';
import { mul } from './test';

function add(x, y) {
  return x + y;
}
// eslint-disable-next-line
console.log(add(1, 5));

// eslint-disable-next-line
console.log(mul(1, 5));
